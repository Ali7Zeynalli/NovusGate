# Database Configuration
POSTGRES_DB=NovusGate
POSTGRES_USER=NovusGate
POSTGRES_PASSWORD=change_me_in_production

# Backend Configuration
DB_NAME=NovusGate
DB_USER=NovusGate
DB_PASSWORD=change_me_in_production
JWT_SECRET=change_me_to_a_random_string_in_production

# API Security
API_KEY=change_me_to_a_random_string_in_production

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_me_in_production

# Network Configuration
# The public IP of your server where WireGuard clients will connect
WG_SERVER_ENDPOINT=127.0.0.1
# The subnet for the Admin VPN network
ADMIN_CIDR=10.99.0.0/24
